import React from "react";

import { ReactComponent as Logo } from "../../assets/bbc-logo.svg";

const Header = () => (
  <header>
    <div>
      <Logo />
    </div>
  </header>
);
export default Header;
